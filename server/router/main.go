package main

import (
	"github.com/charmbracelet/log"
)

func main() {
	log.Info("Server")
	// list_of_connections := nmcli.Connections()
	// for _, conn := range list_of_connections {
	// log.Info(conn)
	// }
	// setting := C.nm_connection_get_setting()
	// log.Info(setting)
	// var client *C.struct__NMClient
	// cancelable := C.struct__GCancellable{}
	// err := &C.struct__GError{}

	// client := C.nm_client_new(nil, nil)
	// version := C.GoString(C.nm_client_get_version(client))
	// active_connections := C.nm_client_get_connections(client)
	// len := int(active_connections.len)
	// log.Infof("Len %d", len)
	// for i := 0; i < len; i++ {
	// 	log.Info(active_connections.pdata)
	// }
	// log.Info(version)
}
