syntax = "proto3";
option go_package = "ras/gen/protobuf/router-internal";

service NetworkInfo {
    rpc GetDevices(DevicesRequest) returns (DevicesResponse){}
}

message Device {
    string iface = 1;
}

message DevicesRequest {}
message DevicesResponse {
    repeated Device interfaces = 1;
}
